# Store your API key in an environment variable or separate config file
# Example usage:
# import os
# API_KEY = os.getenv('OPENAI_API_KEY')

# OR create a separate config.py file (and add it to .gitignore):
# API_KEY = "your-api-key-here"